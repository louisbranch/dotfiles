[[ -s "/home/luiz/.rvm/scripts/rvm" ]] && source "/home/luiz/.rvm/scripts/rvm" # Load RVM into a shell session *as a function*

if [ "$COLORTERM" == "gnome-terminal" ]; then 
  export TERM=xterm-256color 
fi 

if [[ "$TERM" != "screen-256color" ]]
then
  tmux attach-session -t "$USER" || tmux new-session -s "$USER"
  exit
fi

PS1='\w$(__git_ps1 "[%s]")> '
