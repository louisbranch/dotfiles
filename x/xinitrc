# Starts Gnome GMD on boot
# exec ck-launch-session gnome-session
exec i3

# Load custom fonts (Terminus)
xset +fp /usr/share/fonts/local
xset fp rehash

# Active Numlock on boot, requires numlockx
numlockx &
